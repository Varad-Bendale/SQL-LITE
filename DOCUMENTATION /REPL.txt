Lets start with displaying how the stuture of this software is its a like a ms excel but completly based on terminal and its like 
you can put your file in the thing and then you can make the changes in the data powered by the sql queries in the first few lines
you can put your queries it would get executed and then the data would be changed as per those stuff 

Now lets start from the very first thing of getting the input of all the queries its kind of simple I already made the text editor
so I had all the base ready about it I just had to make a new function which like takes all the queries and then put it aside 
So telling in very short its like I made another component in the edit thing bool command for the query for each of the line so that 
if the line is considered as the query it would be the query and it would be positively active for the very first term every single 
time and as we go on and press enter it would go on increasing it size and then you can write the queries over there and those parts
would be highlighted with the white line and then with the pressing the ctrl+e it goes on and gets executed . Like we just stop over
there and dont go forward and give the data which is written in the thing to the temporary buffer we would be needed further on 
